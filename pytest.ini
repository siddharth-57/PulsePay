; STEP 9.3 — Create Pytest Configuration
; What This Step Does: Defines how tests run.
; this file finds the test directory and runs all the tests and checks if they ran successfully without any errors

[pytest]

pythonpath = .

testpaths = tests

python_files = test_*.py

python_classes = Test*

python_functions = test_*